import RegisterForm from "@/src/components/RegisterForm";

export default function RegisterPage() {
    return (
        <div className="flex items-center justify-center min-h-[calc(100vh-70px)] bg-gradient-to-br from-blue-50 to-blue-100">
            <RegisterForm />
        </div>
    );
}